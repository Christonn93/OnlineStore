import { Box } from '@mui/material';
import { Navbar } from '../navigation/Navbar';

const PageHeader = () => {
  return (
    <Box width={'100%'}>
      <Navbar />
    </Box>
  );
};

export default PageHeader;
